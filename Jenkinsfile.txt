pipeline {
    agent any 
   
stages{
	stage('Build'){
            steps {
		sh 'mvn -f HelloWorld/pom.xml clean package'
            }
            post {
                success {
                    echo 'Now Archiving...'
                    archiveArtifacts artifacts: '**/target/*.war'
                }
            }
        }

        stage('buildimage')
	{
            steps 
		{
                sh 'docker build .'
            	}            
        }        
        }
    
}
